let colors =[];

colors.push("green", "red", "blue",);

let numbers = [1, 2, 3, 4, 5,];

colors.pop();

colors.unshift("purple");

console.log("lengths of colors", colors.length);
console.log("length of numbers", numbers.length);